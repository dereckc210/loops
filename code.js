const container = document.getElementByID("fruit")

for(let i = 1 ; i <= 5 ; i++) {
  const p = document.createElement("p");
  p.text.Content = "This is item #" + 1 ; 
container.appendChild(p);
}

let j = 1
while(j <= 5) {
  const p = document.createElement(p);
  p.textContent = "Whileloop item# + 
    container.appendChild(p);
  j++;
}
